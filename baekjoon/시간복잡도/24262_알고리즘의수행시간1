#include <iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    cout << 1 << '\n' << 0;
}



/* 코드
MenOfPassion(A[], n) {
    i = ⌊n / 2⌋;         // 'n/2' 로 i를 구하고
    return A[i]; # 코드1  // 배열 A의 i번째 원소룰 반환
}
// 즉, 어떤 n을 넣더라도 코드는 1회수행, 수행시간은 상수시간 이다. => 시간복잡도는 O(1)
*/
